{
  "name": "contract",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "type": "module",
  "scripts": {
    "build": "near-sdk-js build src/contract.ts build/contract.wasm",
    "deploy": "$npm_execpath run build && near dev-deploy --wasmFile build/contract.wasm",
    "test": "$npm_execpath run build && cd sandbox-ts && $npm_execpath test -- -- \"./build/contract.wasm\""
  },
  "dependencies": {
    "near-cli": "^3.4.2",
    "near-sdk-js": "1.0.0"
  },
  "devDependencies": {
    "typescript": "^4.8.4",
    "ts-morph": "^16.0.0"
  }
}
